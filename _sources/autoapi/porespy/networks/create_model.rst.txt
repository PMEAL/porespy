porespy.networks.create_model
=============================

.. py:function:: porespy.networks.create_model()

   Builds a ResNet50 model for predicting diffusive size factors.

   :returns: **model** -- ResNet50 model built using convolutional and identity blocks.
   :rtype: tensorflow model

