[pytest]
python_files = *.py
python_classes = *Test
python_functions = test_*
doctest_optionflags = ELLIPSIS

addopts =
    --doctest-modules
    --ignore=setup.py

norecursedirs =
    .git
    archive
    bin
    build
    dist
    locals
